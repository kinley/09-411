program p34_1;

var
  s, y: real;
  i, j, k, n: integer;

function Ln(x: real): real;
begin
  x := ln(i + cos * (j * y));
end;

begin
  s := 0;
  readln(n, k, y);
  for i := 2 to n do
  begin
    for j := 0 to k do
    begin
      ln := x;
    end;
  end;
  s := s + x;
  writeln(s);
end.