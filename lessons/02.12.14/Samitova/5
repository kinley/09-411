program pr;
Var a,b,c,y,d,min,max:real;
Begin
 Read(a,b,c);
  if a>b then min:=b
  else min:=a;
  max:=(a+b+c)/3;
   if max<a*b*c then max:=a*b*c;
    if (a*a-b-c)>0 then d:=(a*a-b-c) 
    else d:=-(a*a-b-c);
  y:=max;
  y:=y+min/d;
  Write(y);
End.
